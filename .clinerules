# AI Coding Assistant Rules - Cline / Roo Code / Windsurf

## CRITICAL PROTECTION: PORTA 01 (dyoli)
- **DO NOT MODIFY** any configuration, files, logic, behavior, or assets related to the `dyoli` instance (Estúdio Dyoli Godim, Port 3001).
- This instance is currently running in production and must remain absolutely untouched.
- Any general modifications to the codebase (such as `src/index.ts`, `src/config.ts`, `src/utils.ts`, etc.) must not affect the behavior or compatibility of `dyoli`.
- You are only allowed to modify files or configurations related to `dyoli` if the user explicitly mentions the term "dyoli" or "Porta 01" in the prompt and directs you to modify it. Otherwise, keep it 100% frozen.
